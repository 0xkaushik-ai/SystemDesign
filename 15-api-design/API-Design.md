# API Design

An API is a contract between clients and services. A good API is predictable, secure, versioned, and easy to evolve.

Define endpoints, HTTP methods, request and response schemas, status codes, authentication, pagination, filtering, validation, rate limits, and error formats.

Use idempotency keys for operations such as payments or order creation so retries do not create duplicates. Version APIs carefully and document compatibility rules.
