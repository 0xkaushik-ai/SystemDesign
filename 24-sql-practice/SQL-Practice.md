# SQL Practice

Practice `SELECT`, filtering, joins, grouping, aggregations, subqueries, window functions, indexes, transactions, and query execution plans.

Be able to design tables, choose primary and foreign keys, avoid unnecessary queries, and explain how an index changes read and write performance.
